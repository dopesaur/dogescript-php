such router with $route so
    $frags is (plz explode with '/' $route)
    
    $controller is array_shift($frags)
    $action is array_shift($frags)
    $fragments is $frags
    
    plz var_dump with (plz compact with 'controller' 'action' 'fragments')
wow

plz router with 'abc/def/ghij'